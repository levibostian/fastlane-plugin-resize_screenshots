describe Fastlane::Actions::ResizeScreenshotsAction do
  describe '#run' do
    it 'prints a message' do
      # expect(Fastlane::UI).to receive(:message).with("The resize_screenshots plugin is working!")
      # 
      # Fastlane::Actions::ResizeScreenshotsAction.run(nil)
    end
  end
end
